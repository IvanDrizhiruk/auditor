version: '2'
services:
    auditor-mongodb:
        image: mongo:3.4.8
        ports:
            - "27017:27017"
        command: mongos --configdb auditor-mongodb-config
    auditor-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    auditor-mongodb-config:
        image: mongo:3.4.8
        container_name: auditor-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
